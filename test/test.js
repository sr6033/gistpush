const expect = require('chai').expect;
const main = require('../main');
const exit = require('../success.js');

describe('main()', function() {
	it('should accept credentials, file name & details and return the link to gist(s)', function() {
		
	});
});